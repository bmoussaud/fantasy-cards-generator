# Project Context

- **Owner:** Benoit Moussaud
- **Project:** Web application running on Azure, using AI Services provided by Azure AI Foundry
- **Stack:** TBD — Azure AI Foundry (models/agents), Azure hosting (App Service/Container Apps), frontend framework TBD
- **Created:** 2026-08-26T09:07:51Z

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->
📌 Team update (2026-08-26T09:17:30Z): Initial architecture decisions were proposed in .squad/decisions.md (Azure App Service hosting; Azure AI Foundry text + image model flow), pending user confirmation on open questions.
📌 Team update (2026-08-26T09:30:49Z): Hosting is now standardized on Azure Container Apps by explicit user override, so IaC/deployment work should target Container Apps rather than Azure App Service.
📌 Team policy (2026-08-26T09:34:19Z): Enforce Bicep for all Azure provisioning work; prefer Azure Verified Modules (AVM) when they fit, and fall back to native/custom Bicep only when AVM coverage is insufficient.
📌 Team policy (2026-08-26T09:36:32Z): CI/CD, container builds, and deployment automation should align with Python `uv` workflows, while secrets/settings stay in env vars, Azure Key Vault, or deployment configuration rather than `pyproject.toml`.
📌 Team policy (2026-08-26T09:39:08Z): Use Azure Developer CLI (`azd`) as the standard orchestration layer for provisioning/deployment; scaffold `azure.yaml` and an azd-compatible `infra/` layout alongside the Bicep + AVM baseline when infra work starts.
📌 Team policy (2026-08-26T09:42:05Z): Ensure `.env` remains gitignored and `.env.sample` / `.env.example` exists for local developer setup, while deployed runtime config continues to come from Azure-injected env vars, `azd`, and Key Vault rather than `.env`.
📌 Team policy (2026-08-26T10:04:13Z): Standardize on exactly two `azd` environments — `dev` and `prod` only — with no staging tier for future provisioning and pipeline work.
📌 Team policy (2026-08-26T10:05:50Z): Use Conventional Commits for every future commit in this repo (`<type>[optional scope]: <description>`).
📌 Team update (2026-08-26T12:05:01Z): Added Bicep modules for Cosmos DB serverless (`cards` container only), private Blob Storage (`card-assets`), and Azure AI Foundry account/project/model deployments with Container Apps managed-identity RBAC; exact Foundry model versions/SKUs still need live quota/catalog confirmation.
📌 Work update (2026-08-26T13:11:56.700+00:00): Inspected Bicep defects and opened priority issue #14 for GitHub Copilot with `priority:p1`, `type:bug`, `bug`, `go:yes`, and `squad:copilot`; Copilot capacity was clear and no repository files were changed.
📌 Team update (2026-08-28T08:36:20Z): Fixed the Entra app-registration rollout by wiring `deployEntraAppRegistration` through azd parameters, switching Graph `uniqueName` to a slugged value, and keeping post-provision secret handling additive and non-tracing.
📌 Team update (2026-08-28T08:36:20Z): Diagnosed the dev deployment failure as Azure Container Apps Key Vault secretRef resolution, then restored `azd up` by mirroring `APP_SESSION_SECRET_KEY` and `ENTRA_CLIENT_SECRET` into ACA native secrets while leaving Key Vault as the durable source of record.
📌 Team update (2026-08-31T11:44:29.013+00:00): Deployed mixed-content fix (PR #46 / e62765c) to fcg-dev-app-nat via azd deploy web-nat on 2026-08-31; verified static asset URLs are relative and load over HTTPS.
📌 Work update (2026-08-31T12:12:46.587+00:00): Analyzed deployer access wiring and opened issue #48 (`squad:gimli`) to add Cosmos native Built-in Data Reader at account-root scope and Storage Blob Data Reader at storage-account scope without broadening existing Container App or network permissions.
📌 Team update (2026-08-31T12:18:17.759+00:00): Issue #48 was refined against official Bicep `deployer()` documentation: resolve `deployer().objectId` in root `infra/main.bicep`, remove principal-ID parameter wiring, and retain explicit principal type only for Storage and Foundry ARM role assignments.
📌 Team update (2026-09-01T15:29:35.056+00:00): Diagnosed Azure Container Apps fresh-ACR manifest failure (`dev-1788270781` in `rg-fcag-dev`); fixed infra/main.bicep line 331 to use public `mcr.microsoft.com/azuredocs/containerapps-helloworld:latest` placeholder for initial provision, then `azd deploy` pushes the real image — preserving namePrefix change `fcg→fcag` and passing Bicep build validation.
📌 Team update (2026-09-01T15:47:49.275+00:00): ARM operand-ordering fix diagnosed and formalized by Gandalf. Decision merged into .squad/decisions.md: lead with Integer operands in ARM mathematical functions (mul, div, etc.) to satisfy ARM's stricter type contract; operand reordering is safe and maintains mathematical identity. Applicability: infra/modules/ and all future Bicep compute-variable patterns.
📌 Correction (2026-09-01T16:05:44.296+00:00): User-approved HIGH-confidence correction — operand reordering does NOT resolve ARM float rejection. ARM's `mul()` rejects Float operands regardless of position. Action: (1) avoid ARM float multiplication; (2) compute thresholds in KQL using `dailyQuotaGb * ingestionWarningPercent / 100.0`; (3) require `azd provision --preview` for ARM expression changes, since local Bicep build/lint does not catch runtime template validation constraints. Supersedes prior operand-reordering guidance.
📌 Incident fix (2026-09-01T17:19:00.962+00:00): APP_SESSION_SECRET_KEY preprovision hook deployed. Root cause: azd env lacked the secret key; empty default in azd parameters silently passed empty string to Bicep, which gated both ACA native secret and env secretRef on non-empty condition. Fix: Added idempotent `preprovision` hook that generates and stores (via `azd env set`, no logging) a cryptographically secure value only when absent. Result: Container App revision Running/Healthy with `/healthz` HTTP 200. Secret value never printed or exposed. Key Vault sync handled by deployer principal on next provision.
📌 Team update (2026-09-02T14:10:23+0000): Completed issue #52 in PR #57 by granting the azd deployer Key Vault Reader at vault scope from `deployer().objectId`; tests/docs/validation all passed.
📌 Team update (2026-09-02T16:31:07.441+00:00): The authenticated card library uses read-only 5-minute user-delegation Blob SAS URLs for artwork, so infra/runtime access must pair container-scoped Blob Data Contributor with account-scoped Storage Blob Delegator for SAS signing.
📌 Team update (2026-09-04T13:10:31.843+00:00): Gandalf published an architecture spec at docs/architecture-agents-foundry.md proposing a Foundry-hosted card-orchestrator agent (Microsoft Agent Framework); review for your domain's implications (backend integration / infra RBAC+Bicep / UI states / test strategy).
